import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Welcome to TCG Scanner")
                .font(.title)
            Spacer()
        }
        .padding()
    }
}
